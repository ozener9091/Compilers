## Список выполненных работ

- [Лабораторная работа 1.](#lab1)
- [Лабораторная работа 2.](#lab2)
- [Лабораторная работа 3.](#lab3)
- [Лабораторная работа 4.](#lab4)
- [Лабораторная работа 5.](#lab5)

## <a id="lab1"></a> Лабораторая работа №1 Разработка пользовательского интерфейса (GUI) для языкового процессора

### 🎯 Цель работы
Создание кроссплатформенного графического интерфейса (GUI) для языкового процессора в виде специализированного текстового редактора.

### 👨‍💻 Автор
**Ситников В.И.**
Группа: АП-326

### 📋 Описание проекта
**GUI для языкового процессора на JavaFX**

Данный проект представляет собой графический интерфейс для языкового процессора, реализованный в виде специализированного текстового редактора. Приложение обеспечивает удобный интерфейс для работы с текстом и базовые функции редактирования.

### 🛠 Используемые технологии
| Технология | Назначение |
|------------|------------|
| **Java 25** | Основной язык программирования |
| **JavaFX 21** | Фреймворк для создания графического интерфейса |
| **IntelliJ IDEA** | Среда разработки |
| **Git** | Система контроля версий |
| **Maven** | Сборка проекта и управление зависимостями |

### 💾 Ссылка для скачивания
Скачать программу (Только GUI): [CompilersLab1-v1.0.zip](https://github.com/ozener9091/Compilers_Laba1/releases/download/v1.0/CompilersLaba1.zip)

### 🚀 Инструкция по сборке и запуску

#### 📌 Требования
- Операционная система: Windows / macOS / Linux
- JDK 25 или выше
- Git (опционально)

#### 🔧 Пошаговая инструкция

**1. Установка JDK 25**

Скачайте JDK 25 с [официального сайта Oracle](https://www.oracle.com/java/technologies/downloads/)

**2. Настройка переменных окружения**

*Windows:*
```bash
# Добавьте в PATH:
C:\Program Files\Java\jdk-25\bin

# Проверьте установку:
java -version
javac -version
```

*MacOs/Linux:*
```bash
export JAVA_HOME=/путь/к/jdk-25
export PATH=$JAVA_HOME/bin:$PATH
```

**2. Клонирование и запуск**
```bash
# Клонирование репозитория
git clone https://github.com/your-repo/compilers-laba1.git
cd compilers-laba1

# Компиляция и запуск
javac Launcher.java
java Launcher
```

Запуск через IntelliJ IDEA:

    1. Откройте проект
    2. Настройте JDK 25 (File → Project Structure → SDK)
    З. апустите через Run → Run 'Launcher'

Запуск через Maven:
```bash
mvn clean compile
mvn javafx:run
```
**3. 🖥 Описание интерфейса и функций**
Базовый функционал:

    ✅ Открытие текстовых файлов (.txt)

    ✅ Сохранение файлов

    ✅ Создание нового документа

    ✅ Копирование/Вставка/Вырезание текста

    ✅ Отмена действий

    ✅ Масштабируемые области с ползунками (SplitPane)

### Руководство пользователя

**1. Интерфейс**

<img width="1919" height="1120" alt="изображение" src="https://github.com/user-attachments/assets/cc677632-30c9-473d-a358-9971ab600209" />

Интерфейс делится на 5 основных блока:
1. Меню
2. Панель инструментов
3. Редактор
4. Вывод
5. Ошибки

**2. Меню**

<img width="449" height="41" alt="изображение" src="https://github.com/user-attachments/assets/519eca0b-2cb6-44d1-9e00-7422615c0f4e" />

Меню состоит из 4 вкладок:
1. Файл
2. Правка
3. Справка
4. Язык

Вкладка "Файл"

<img width="157" height="252" alt="изображение" src="https://github.com/user-attachments/assets/4cee3a11-af46-404d-bec8-0508657a5126" />

1. Создать - Создать новый документ
2. Открыть - Открыть существующий документ
3. Сохранить - Сохранить документ
4. Сохранить как - Сохранить документ с выбором директории
5. Выход - Выйти из программы

Вкладка "Правка"

<img width="140" height="281" alt="изображение" src="https://github.com/user-attachments/assets/f07b404c-e363-4fdd-ac63-4a851043ed74" />

1. Отменить - Отменить предыдущее действие (Ctrl+Z)
2. Вырезать - Удалить выделенный текст и сохранить в буффер (Ctrl+X)
3. Копировать - Копировать выделенный текст в буффера
4. Вставить - Вставить текст из буффера
5. Удалить - Удалить выделенный текст
6. Выделить все - Выделить весь текст

Вкладка "Справка"

<img width="252" height="124" alt="изображение" src="https://github.com/user-attachments/assets/2c9e6599-957d-4340-ac69-87600b94f7d1" />

1. Руководство пользователя - ссылка на GitHub репозиторий
2. О программе - Информация о программе и авторе

Вкладка "Язык"

<img width="155" height="132" alt="изображение" src="https://github.com/user-attachments/assets/9ab34aad-b85a-4f52-a379-c5c47977f066" />

1. Русский - Выбрать русский язык
2. Английский - Выбрать английский язык

**3. Панель инструментов**

<img width="542" height="81" alt="изображение" src="https://github.com/user-attachments/assets/a3d6ef31-334b-4823-916c-5bb8b24b7050" />

Повторяет пункты меню
1. Создать
2. Открыть
3. Сохранить
4. Отменить
5. Копировать
6. Вырезать
7. Вставить

**4. Редактор**

<img width="1914" height="597" alt="изображение" src="https://github.com/user-attachments/assets/9ef80bc3-9783-4104-a3d0-be7db3bd6123" />

Имеет подсветку синтаксиса на ключевые слова while if int и т.д.<br>
Имеет возможность менять масштаб с помощью кнопок + - в правой нижней части редактора.<br>
Имеет возможность редактировать несколько файлов.<br>

**5. Поле вывода**

<img width="1912" height="276" alt="изображение" src="https://github.com/user-attachments/assets/8d12bbd3-59f9-4a46-8fbc-d7384d1d1511" />

Выводит результат отработки программы.<br>
Имеет меню для выбора модуля.<br>
Имеет возможность менять масштаб с помощью кнопок + - в провой нижней части редактора.<br>
Имеет ползунок для смены высоты окна.<br>

**6. Таблица с ошибками**

<img width="1913" height="241" alt="изображение" src="https://github.com/user-attachments/assets/b2d07a8e-6107-47a5-8e26-95f0a6053c58" />

Содержит 3 столбца
1. Тип - тип ошибки
2. Содержание - в чем заключается ошибка
3. Номер страницы - на какой строке произошла ошибка


## <a id="lab2"></a> Лабораторая работа №2 Разработка лексического анализатора (сканера)


### 🎯 Цель работы
Изучить назначение и принципы работы лексического анализатора в структуре компилятора. Спроектировать алгоритм (диаграмму состояний) и выполнить программную реализацию сканера для выделения лексем из входного текста. Интегрировать разработанный модуль в ранее созданный графический интерфейс языкового процессора.

### 👨‍💻 Автор
**Ситников В.И.**
Группа: АП-326

### 📋 Постановка задачи

Разработать лексический анализатор (сканер) в соответствии с индивидуальным вариантом задания, интегрировать его в приложение из лабораторной работы №1 и обеспечить наглядный вывод результатов.

Требования к реализации сканера:

 1. Спроектировать диаграмму состояний конечного автомата, реализующего сканер, согласно варианту задания.

 2. Разработать программный модуль лексического анализа, который:

    принимает на вход строку (исходный текст программы);

    выделяет все допустимые лексемы согласно варианту;

 3. Классифицирует лексемы по типам (например: "ключевое слово", "идентификатор", "число", "оператор", "разделитель").

 4. Любые символы, не соответствующие ни одному из допустимых типов лексем, считать недопустимыми и выводить сообщение об ошибке с указанием позиции.

 5. Учитывает многострочность входного текста.


### 📌 Вариант

Лямбда-выражения языка Python
> calc = lambda a, b, c: a + (b * c);


Допустимые лексемы

| Лексема | Код |
|------------|------------|
| **Идентификатор (id)** | 1 |
| **Ключевое слово lambda** | 2 |
| **Знак присваивания (=)** | 3 |
| **Знак запятой (,)** | 4 |
| **Двоеточие (:)** | 5 |
| **Знак сложения (+)** | 6 |
| **Открывающая скобка** | 7 |
| **Закрывающая скобка** | 8 |
| **Знак умножения (*)** | 9 |
| **Разделитель (пробел)** | 10 |
| **Знак конца команды (;)** | 11 |

Пример корректных строк

> a = lambda a, b: a * b;

> mab = lambda sdf, fdg: sdf + (sdf + fdg);

### 🖥 Конечный автомат



![Диаграмма автомата](img/ДиаграммаАвтомата.png)

На диаграмме выделены допустимые лексемы и их условные кода.


### 🚀 Тестовые примеры

Тест 1 - Корректная строка

![Диаграмма автомата](img/test1.png)

Тест 2 - Некорректная строка

![Диаграмма автомата](img/test2.png)

Тест 3 - Многострочная строка

![Диаграмма автомата](img/test3.png)

### Дополнительное задание (Flex&Bison)

Т.к. требуется сделать только лексический анализатор, будем использовать только flex

Определение токенов для flex

>#define ID          1
>
>#define LAMBDA      2
>
>#define ASSIGN      3
>
>#define COMMA       4
>
>#define COLON       5
>
>#define PLUS        6
>
>#define LPAREN      7
>
>#define RPAREN      8
>
>#define MULT        9
>
>#define SPACE       10
>
>#define SEMICOLON   11


Пример работы (консоль)

![Тест flex1](img/flex1.jpg)


Пример работы (приложение)

![Тест flex2](img/flex2.jpg)




## <a id="lab3"></a>Лабораторая работа №3 Разработка синтаксического анализатора (парсера)

### 🎯 Цель работы
Изучить назначение и принципы работы синтаксического анализатора в структуре компилятора. Спроектировать грамматику, построить соответствующую схему метода анализа грамматики и выполнить программную реализацию парсера с нейтрализацией синтаксических ошибок методом Айронса. Интегрировать разработанный модуль в ранее созданный графический интерфейс языкового процессора.

### 👨‍💻 Автор
**Ситников В.И.**
Группа: АП-326

### 📋 Постановка задачи

Разработать синтаксический анализатор (парсер) в соответствии с индивидуальным вариантом курсовой (расчетно-графической) работы, интегрировать его в приложение из лабораторной работы №1 и обеспечить наглядный вывод результатов анализа.

Требования к разработке парсера:

- Разработать грамматику для заданной синтаксической конструкции.

- Построить схему метода анализа на основе разработанной грамматики.

- Выполнить программную реализацию алгоритма работы синтаксического анализа.

- Реализовать алгоритм нейтрализации синтаксических ошибок методом Айронса.

- Входные данные — строка (текст программного кода) из области редактирования.

- Выходные данные:

        - При успешном анализе корректной строки — сообщение об отсутствии ошибок.

        - При обнаружении ошибок — таблица с описанием каждой ошибки.


### 📌 Вариант

Лямбда-выражения языка Python
> calc = lambda a, b, c: a + (b * c);


Допустимые лексемы

| Лексема | Код |
|------------|------------|
| **Идентификатор (id)** | 1 |
| **Ключевое слово lambda** | 2 |
| **Знак присваивания (=)** | 3 |
| **Знак запятой (,)** | 4 |
| **Двоеточие (:)** | 5 |
| **Знак сложения (+)** | 6 |
| **Открывающая скобка** | 7 |
| **Закрывающая скобка** | 8 |
| **Знак умножения (*)** | 9 |
| **Разделитель (пробел)** | 10 |
| **Знак конца команды (;)** | 11 |

Пример корректных строк

> a = lambda a, b: a * b;

> mab = lambda sdf, fdg: sdf + (sdf + fdg);

### 🖥 Разработка грамматики

Z -> <идентификатор> '=' <лямбда-выражение>';'

<идентификатор> -> letter <идентификатор-продолжение>

<идентификатор-продолжение> -> letter|digit|sybmol <идентификатор-продолжение> | e

<лямбда-выражение> -> 'lambda ' <параметры>: <выражение>

<параметры> -> e | <идентификатор> ',' <параметры> | <идентификатор>

<выражение> -> <выражение> '+' <терм> | <выражение> '-' <терм> | <терм>ф

<терм> -> <терм> '*' <фактор> | <терм> '/' <фактор> | <фактор>

<фактор> -> '('<фактор>')' | <идентификатор> | digit


Vт = {'=', ';', 'lambda ', ',', '+', '-', '*', '/', '(', ')' letter, digit}

Vн = {<идентификатор>, <лямбда-выражение>, <идентификатор-продолжение>, <параметры>, <выражение>, <терм>, <фактор>


### 🚀 Классификация грамматики (по Хомскому)

Данная грамматика относится к контекстно-свободной грамматике.

Здесь в левой части правил может быть только один нетерминальный
символ, а в правой – любая последовательность из терминалов и
нетерминалов, что соответствует разработанной мною грамматике.

### Метод анализа

Т.к. грамматика контекстно-свободная для анализа будет использоваться метод рекурсивного спуска.

Основная идея этого метода состоит в том, что каждому нетерминалу грамматики ставится
в соответствие определенная программная единица, процедура или функция
(например в языке С – функция), которая распознает цепочку, порождаемую 
этим нетерминалом. Такие процедуры (функции) вызываются в соответствии с правилами
грамматики, причем иногда вызывают сами себя рекурсивно. 
Следовательно, языком реализации этого метода может быть лишь такой язык, который допускает рекурсию.

Полученная диаграмма рекурсивного спуска.

![Диаграмма спуска](img/ДиаграммаСпуска.png)

### Диагностика и нейтрализация синтаксических ошибок

Для диагностики и нейтрализации синтаксических ошибок использовался метод Айронса.

Основная идея – по контексту без возврата отбрасывать литеры, которые привели к тупиковой 
ситуации (когда продолжение анализа по грамматике невозможно), и продолжать разбор. 

### Тестовые примеры

Пример корректного ввода строки

![Тест4](img/test4.jpg)

Пример некорректного ввода строки

![Тест5](img/test5.jpg)

Пример многострочного ввода

![Тест6](img/test6.jpg)


### Дополнительное задание

В соответствии с вариантом задания на курсовую работу необходимо:

- Cгенерировать код лексера и парсера для анализа грамматики с помощью программного обеспечения ANTLR, а затем внедрить и протестировать в программе, реализованной в рамках первой лабораторной работы.

- Дополнить README-файл грамматиками (1я - разработанная Вами и 2я - переписанная для ПО ANTLR) и тестовыми примерами (реализация с помощью ANTLR).

### Тестовые примеры ANTLR

Пример корректной строки

![Тест7](img/test7.jpg)

Пример некорректной строки

![Тест8](img/test8.jpg)

Пример многострочного ввода

![Тест9](img/test9.jpg)


### Грамматика разработнная мною:


Z -> <идентификатор> '=' <лямбда-выражение>';'

<идентификатор> -> letter <идентификатор-продолжение>

<идентификатор-продолжение> -> letter|digit|sybmol <идентификатор-продолжение> | e

<лямбда-выражение> -> 'lambda ' <параметры>: <выражение>

<параметры> -> e | <идентификатор> ',' <параметры> | <идентификатор>

<выражение> -> <выражение> '+' <терм> | <выражение> '-' <терм> | <терм>ф

<терм> -> <терм> '*' <фактор> | <терм> '/' <фактор> | <фактор>

<фактор> -> '('<фактор>')' | <идентификатор> | digit


Vт = {'=', ';', 'lambda ', ',', '+', '-', '*', '/', '(', ')' letter, digit}

Vн = {<идентификатор>, <лямбда-выражение>, <идентификатор-продолжение>, <параметры>, <выражение>, <терм>, <фактор>

### Автомат для ANTLR (Лексер):

```
lexer grammar LambdaLexer;

@header {
package antlr;
}

LAMBDA: 'lambda';

ASSIGN: '=';
COMMA: ',';
COLON: ':';
PLUS: '+';
MINUS: '-';
STAR: '*';
SLASH: '/';
LPAREN: '(';
RPAREN: ')';
SEMICOLON: ';';

IDENTIFIER: LETTER (LETTER | DIGIT | '_')*;

NUMBER: DIGIT+;

WS: [ \t\r\n]+ -> skip;

UNKNOWN: .;

fragment LETTER: [a-zA-Z];
fragment DIGIT: [0-9];
```


### Грамматика для ANTLR (парсер):

```
parser grammar LambdaParser;

options { tokenVocab=LambdaLexer; }

@header {
package antlr;
}

program: statement+ EOF;

statement: identifier ASSIGN lambdaExpression SEMICOLON;

lambdaExpression: LAMBDA parameters COLON expression;

parameters: (identifier (COMMA identifier)*)?;

expression: term ((PLUS | MINUS) term)*;

term: factor ((STAR | SLASH) factor)*;

factor: NUMBER
      | identifier
      | LPAREN expression RPAREN
      ;

identifier: IDENTIFIER;
```

## <a id="lab4"></a> Лабораторая работа №4 Реализация алгоритма поиска подстрок с помощью регулярных выражений

### 🎯 Цель работы
Изучить теоретические основы регулярных выражений и их применение для поиска и извлечения подстрок из текста. Освоить практические навыки использования библиотечных средств работы с регулярными выражениями, а также интеграцию алгоритмов поиска в графический интерфейс приложения.

### 👨‍💻 Автор
**Ситников В.И.**
Группа: АП-326

### 📌 Постановка задачи

Разработать модуль поиска подстрок с использованием регулярных выражений, интегрировать его в существующее приложение (текстовый редактор) и обеспечить наглядный вывод результатов.
Студент получает индивидуальный вариант, содержащий 3 задачи на поиск подстрок определенных форматов.

 - Построить РВ для поиска идентификатора, который может начинаться только с буквы a-zA-Z, 
знака доллара $ или подчеркивания _, оставшаяся часть символов идентификатора представляют собой только цифры. 


 - Построить РВ, описывающее имя пользователя (набор букв и цифр длиной 2-30 символов, первым символом должна быть буква). 


 - Построить РВ, описывающее долготу в формате градусы/минуты/секунды, например 122°30'15"W (учесть диапазон корректных значений).

### Решение

#### Пункт 1

a) Построить РВ для поиска идентификатора, который может начинаться только с буквы a-zA-Z, 
знака доллара $ или подчеркивания _, оставшаяся часть символов идентификатора представляют собой только цифры.

b) Выражение: [a-zA-Z_$][0-9]*

Объяснение - начинается с 1 буквы или знака _ или $ (от z до Z + _ + $), затем цифры (от 0 до 9) и * значит их может быть много

c) Примеры строк, которые должны находиться

 - z234923890
 - z
 - _32432
 -$324324

d) Примеры строк, которые не должны находиться

 - 324534543
 - №303234
 - *3924923

e) Тестовые примеры (скриншоты)

![Тест10](img/test4.png)

#### Пункт 2

a) Построить РВ, описывающее имя пользователя (набор букв и цифр длиной 2-30 символов, первым символом должна быть буква). 

b) Выражение: [a-zA-Z][a-zA-Z0-9]{1,29}

Объяснение - начинается с 1 буквы, затем либо буквы либо цифры с ограниченной длиной 29 

c) Примеры строк, которые должны находиться

 - z23492dsf3890
 - z
 - A3243aas2
 - SAB324324

d) Примеры строк, которые не должны находиться

 - 3245sss4543
 - №30323d4
 - 3924923

e) Тестовые примеры (скриншоты)

![Тест11](img/test5.png)

#### Пункт 3

a) Построить РВ, описывающее долготу в формате градусы/минуты/секунды, например 122°30'15"W (учесть диапазон корректных значений).

b) Выражение: (?:1[0-7][0-9]|[1-9]?[0-9])°[0-5][0-9]'[0-5][0-9]"[EW]

Объяснение - (?:1[0-7][0-9]|[1-9]?[0-9]) - диапазон чисел либо от 100 до 179 либо от 1 до 99

[0-5][0-9] - диапазон чисел от 0 до 59

[EW] - в конце либо E (восточная долгота) либо W (северная долгота)

c) Примеры строк, которые должны находиться

 - 122°30'15"W
 - 55°05'05"E

d) Примеры строк, которые не должны находиться

 - 122°303'155"W
 - °30'15"
 - 222°60'100"W

e) Тестовые примеры (скриншоты)

![Тест12](img/test6.png)

### Дополнительное задание

Вариант: 

Построить РВ, описывающее имя пользователя (набор букв и цифр длиной 2-30 символов, первым символом должна быть буква). 

Граф автомата для регулярного выражения

![ГрафРегуляр](img/ГрафРегуляр.png)


Пример переходов для строки ozener9091

```
START -> letter 1_symb
1_symb -> letter 2_symb
2_symb -> letter 3_symb
3_symb -> letter 4_symb
4_symb -> letter 5_symb
5_symb -> letter 6_symb
6_symb -> digit 7_symb
7_symb -> digit 8_symb
8_symb -> digit 9_symb
9_symb -> digit 10_symb
10_symb -> e END
```


## <a id="lab5"></a> Лабораторная работа 5: Построение AST и семантический анализ

**Автор:** Ситников В.И.  

## Вариант задания
Обрабатываемая конструкция:

```python
calc = lambda a, b, c: a + (b * c);
```

Примеры корректных строк:

```python
calc = lambda a, b, c: a + (b * c);
sum2 = lambda x, y: x + y;
id = lambda z: z;
```

## Реализованные контекстно-зависимые условия

1. **Уникальность идентификаторов**
- Проверяется повторное объявление имени функции в глобальной области.
- Проверяется повторное объявление имени параметра в области параметров лямбда-выражения.
- Пример:

```python
calc = lambda a: a;
calc = lambda b: b;
```

Ожидаемое сообщение:
- `Семантическая ошибка: Идентификатор "calc" уже объявлен ранее в этой области видимости. (строка 2, символ ...)`

2. **Совместимость типов**
- Для арифметических операций `+`, `-`, `*`, `/` оба операнда должны быть типа `Int`.
- Использование имени ранее объявленной лямбда-функции как числа считается несовместимостью типов.
- Пример:

```python
f = lambda x: x;
g = lambda a: a + f;
```

Ожидаемое сообщение:
- `Семантическая ошибка: Несовместимость типов: арифметические операторы применимы только к типу Int.`

3. **Допустимые значения**
- Числовой литерал проверяется на попадание в диапазон `Int` (`Integer.MIN_VALUE ... Integer.MAX_VALUE`).
- Пример:

```python
calc = lambda: 999999999999999999999999;
```

Ожидаемое сообщение:
- `Семантическая ошибка: Числовой литерал "..." выходит за допустимые пределы типа Int.`

4. **Использование идентификаторов**
- Идентификатор в выражении должен быть объявлен ранее (параметр текущей лямбды или ранее объявленная функция).
- Пример:

```python
calc = lambda a: a + b;
```

Ожидаемое сообщение:
- `Семантическая ошибка: Идентификатор "b" используется до объявления.`

## Структура AST

Типы узлов:
- `ProgramNode` — корень программы.
- `LambdaDeclNode` (`LambdaDeclarationNode`) — объявление лямбда-функции.
- `ParameterNode` — параметр функции.
- `BinaryOpNode` — бинарная операция.
- `IdentifierNode` — идентификатор.
- `IntLiteralNode` (`NumberLiteralNode`) — целочисленный литерал.

Пример AST (текстовый вывод):

```text
ProgramNode
└── LambdaDeclNode
    ├── name: "calc"
    ├── parameters:
    ├── ParameterNode
    │   └── name: "a"
    ├── ParameterNode
    │   └── name: "b"
    ├── ParameterNode
    │   └── name: "c"
    ├── body:
    └── BinaryOpNode
        ├── operator: "+"
        ├── IdentifierNode
        │   └── name: "a"
        └── BinaryOpNode
            ├── operator: "*"
            ├── IdentifierNode
            │   └── name: "b"
            └── IdentifierNode
                └── name: "c"
```

### CST / AST (схема для корректной строки)

```mermaid
graph TD
    A[Program] --> B[LambdaDecl: calc]
    B --> C[Parameters]
    C --> C1[a]
    C --> C2[b]
    C --> C3[c]
    B --> D[Body]
    D --> E[+]
    E --> F[a]
    E --> G[*]
    G --> H[b]
    G --> I[c]
```

## Формат вывода в программе

После нажатия `Пуск`:
- В поле `AST (текстовый вид)` отображается AST только для семантически корректных объявлений.
- В поле `Сообщения об ошибках` отображаются ошибки с позицией: строка и символ.
- Отдельно отображается строка `Количество ошибок: N`.

Дополнительно:
- Кнопка `Показать AST` открывает отдельное графическое окно AST.

## Графическая визуализация AST

Использованы средства JavaFX:
- `TreeView<String>` для отображения узлов AST.
- `TreeItem<String>` для построения иерархии узлов (родитель -> потомок).
- Отдельное окно (`Stage`) открывается по кнопке `Показать AST`.

Каждый узел отображается с типом и ключевыми атрибутами:
- `LambdaDeclNode [name=...]`
- `ParameterNode [name=...]`
- `BinaryOpNode [op=...]`
- `IdentifierNode [name=...]`
- `IntLiteralNode [value=...]`




